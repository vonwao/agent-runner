# Simple Hello World Test

## Goal
Create a trivial utility function to test the basic Runr workflow end-to-end.

## Requirements
- Add a new file: `src/utils/greet.ts`
- Export a simple function: `greet(name: string): string`
- Returns: `"Hello, {name}!"`
- No dependencies, pure function

## Success Criteria
- File created with function
- TypeScript compiles (`npm run build` passes)
- Function works as specified

## Scope
- Single file only
- No tests needed (it's trivial)
- Just demonstrate workflow
