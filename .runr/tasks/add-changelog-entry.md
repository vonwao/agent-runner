# Add Meta-Agent UX Sprint to Changelog

## Goal
Document the Meta-Agent UX Sprint features in CHANGELOG.md

## Requirements
- Add new section for v0.6.0 (unreleased)
- List all new features:
  - `runr meta` command
  - `--with-claude` flag for init
  - `.claude/` integration templates
  - Enhanced `runr doctor` with meta-agent checks
- Use existing changelog format/style
- Add migration notes if needed

## Success Criteria
- CHANGELOG.md updated with v0.6.0 section
- All major features documented
- Format matches existing entries
- No typos or formatting issues

## Scope
- Only modify CHANGELOG.md
- Don't bump version in package.json (separate task)
